<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:system="clr-namespace:System;assembly=mscorlib"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               xmlns:loc="clr-namespace:DmToolsApp.Extensions"
               xmlns:dialogs="clr-namespace:DmToolsApp.Components.Dialogs"
               x:TypeArguments="system:Boolean"
               x:Class="DmToolsApp.Components.Dialogs.ChannelSettingsDialog"
               x:DataType="dialogs:ChannelSettingsDialogViewModel"
               BackgroundColor="Transparent"
               Padding="0">
    <Border Style="{StaticResource DialogCardBorder}" WidthRequest="320">
        <VerticalStackLayout Spacing="16">
            <Label Text="{loc:Loc ChannelSettings}"
                   FontAttributes="Bold" FontSize="{OnIdiom Default=16, Desktop=14}"
                   TextColor="{DynamicResource AppText}"/>
            <Label Text="{Binding TrackName}"
                   FontSize="{OnIdiom Default=14, Desktop=12}"
                   TextColor="{DynamicResource AppTextMuted}"
                   Style="{StaticResource TruncatedLabelStyle}"/>

            <!-- Volume -->
            <Grid ColumnDefinitions="auto,*,40" ColumnSpacing="10">
                <Label Text="{loc:Loc SceneVolume}" FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center"/>
                <Slider Grid.Column="1"
                        Minimum="0" Maximum="1"
                        Value="{Binding Volume, Mode=TwoWay}"/>
                <Label Grid.Column="2"
                       Text="{Binding VolumePercent}"
                       FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center" HorizontalTextAlignment="End"/>
            </Grid>

            <!-- Toggles -->
            <Grid ColumnDefinitions="*,auto" RowDefinitions="auto,auto,auto,auto" RowSpacing="4">
                <Label Grid.Row="0" Text="{loc:Loc ChannelLoop}" FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center"/>
                <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding IsLooping, Mode=TwoWay}"/>

                <Label Grid.Row="1" Text="{loc:Loc SceneFadeIn}" FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center"/>
                <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding FadeIn, Mode=TwoWay}"/>

                <Label Grid.Row="2" Text="{loc:Loc SceneFadeOut}" FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center"/>
                <CheckBox Grid.Row="2" Grid.Column="1" IsChecked="{Binding FadeOut, Mode=TwoWay}"/>

                <Label Grid.Row="3" Text="{loc:Loc SceneAuto}" FontSize="{OnIdiom Default=14, Desktop=12}" VerticalOptions="Center"/>
                <CheckBox Grid.Row="3" Grid.Column="1" IsChecked="{Binding AutoPlay, Mode=TwoWay}"/>
            </Grid>

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0"
                        Text="{loc:Loc BtnCancel}"
                        Style="{StaticResource DialogSecondaryButton}"
                        Command="{Binding CancelCommand}"/>
                <Button Grid.Column="1"
                        Text="{loc:Loc BtnSave}"
                        Command="{Binding SaveCommand}"/>
            </Grid>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>
